/**
 * Dev-mode entry point (Vite dev server only).
 * The IIFE production build uses src/moduleEntry.ts instead.
 */
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
